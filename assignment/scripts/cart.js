console.log('***** Cart Functions *****');
// Make sure to test all functions here in the JS file!
// We want to see how you are testing your code!!!

let basket = []

function addItem {
  //Take an input parameter for a string 'item'
  //add the new item to the global array 'basket'
  //return true to verify the item was added
}

function listItems {
  //loop through the 'basket' array and
  //log each item on a new line in the console

}

function empty {
  //reset the 'basket' array so it is empty
}
